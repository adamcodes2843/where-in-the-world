interface cardProps {
    flag: string,
    name: string,
    population: number,
    region: string,
    capital: string
}

const Card = ({flag, name, population, region, capital}: cardProps) => {
  return (
    <>

    </>
  )
}

export default Card